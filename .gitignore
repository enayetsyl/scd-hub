# ---- Secrets & env (NEVER commit) ----
.env
.env.*
!.env.example
*.pem
*.key
secrets/
# Google OAuth client downloads (GP-A creds live in .env only, D-#70/#71)
client_secret*.json
*.apps.googleusercontent.com.json

# ---- Node / JS ----
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
dist/
build/
coverage/

# ---- Expo / React Native ----
.expo/
.expo-shared/
web-build/
dist-web/
# Built APKs and Android signing material (keystore lives only on the build machine + backup)
*.apk
keystore.properties
*.jks
*.p8
*.p12
*.mobileprovision
*.orig.*

# ---- Python (import harness) ----
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/

# ---- Real student/guardian PII (NEVER commit — ADR-005) ----
# Source roster exports and any normalized JSON the import scripts produce.
*.xlsx
*.xls
server/scripts/students.json
server/scripts/staff.json
_xlsx_dump.txt
_xlsx_vals.txt

# ---- Editor / OS ----
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.log

# ---- Claude Code ----
# Ignore everything except the shared project settings (hooks live there and
# must be present in worktree checkouts); local overrides + worktrees stay out.
.claude/*
!.claude/settings.json

# ---- Local-only operator notes (VM access etc. — IP/creds never commit, deployment §0) ----
*.local.md
