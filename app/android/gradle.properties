# Project-wide Gradle settings.

# IDE (e.g. Android Studio) users:
# Gradle settings configured through the IDE *will override*
# any settings specified in this file.

# For more details on how to configure your build environment visit
# http://www.gradle.org/docs/current/userguide/build_environment.html

# Specifies the JVM arguments used for the daemon process.
# The setting is particularly useful for tweaking memory settings.
# Default value: -Xmx512m -XX:MaxMetaspaceSize=256m
org.gradle.jvmargs=-Xmx2048m -XX:MaxMetaspaceSize=512m

# When configured, Gradle will run in incubating parallel mode.
# This option should only be used with decoupled projects. More details, visit
# http://www.gradle.org/docs/current/userguide/multi_project_builds.html#sec:decoupled_projects
# org.gradle.parallel=true

# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true

# Automatically convert third-party libraries to use AndroidX
android.enableJetifier=true

# Enable AAPT2 PNG crunching
android.enablePngCrunchInReleaseBuilds=true

# Use this property to specify which architecture you want to build.
# You can also override it from the CLI using
# ./gradlew <task> -PreactNativeArchitectures=x86_64
# MUST keep armeabi-v7a: 32-bit Android Go devices (e.g. Galaxy A03 Core / Unisoc
# SC9863A) can ONLY load armeabi-v7a native libs; an arm64-only APK installs but
# crashes on launch there ("opens then closes"). Restored to the Expo default set
# (was trimmed to arm64-only for faster local builds — that regression shipped).
reactNativeArchitectures=armeabi-v7a,arm64-v8a,x86,x86_64

# Use this property to enable support to the new architecture.
# This will allow you to use TurboModules and the Fabric render in
# your application. You should enable this flag either if you want
# to write custom TurboModules/Fabric components OR use libraries that
# are providing them.
newArchEnabled=false

# Use this property to enable or disable the Hermes JS engine.
# If set to false, you will be using JSC instead.
hermesEnabled=true

# Enable GIF support in React Native images (~200 B increase)
expo.gif.enabled=true
# Enable webp support in React Native images (~85 KB increase)
expo.webp.enabled=true
# Enable animated webp support (~3.4 MB increase)
# Disabled by default because iOS doesn't support animated webp
expo.webp.animated=false

# Enable network inspector
EX_DEV_CLIENT_NETWORK_INSPECTOR=true

# Use legacy packaging to compress native libraries in the resulting APK.
expo.useLegacyPackaging=false

# compileSdk/buildTools are DELIBERATELY left at the build.gradle defaults (34 / 34.0.0),
# which is what Expo SDK 51 expects. A local override to 35 used to live here because this
# laptop lacked the android-34 platform — it caused two real failures and has been removed
# after installing `platforms;android-34`:
#   1. FATAL in prod: API 35 android.jar declares java.util.List.removeLast(), so Kotlin bound
#      the interface MEMBER in react-native-screens instead of its stdlib extension, and the
#      method does not exist below Android 15 (NoSuchMethodError, 26 events / 4 users).
#   2. Build break: API 35 marks PackageInfo.requestedPermissions @Nullable, which does not
#      compile in expo-modules-core 1.12.26 (PermissionsService.kt:166).
# Do not raise compileSdk here without checking both.
# JDK 17 toolchain (RN 0.74 requires it). Point Gradle at the local install and never
# try to auto-download one from GitHub (this laptop's ISP resets those transfers).
org.gradle.java.installations.auto-download=false
org.gradle.java.installations.paths=C:\\Users\\HP\\jdk17\\jdk-17.0.13+11
